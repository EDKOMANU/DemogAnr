% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pf_ratio.R
\name{pf_ratio}
\alias{pf_ratio}
\title{Brass P/F Ratio Method for Fertility Estimation}
\usage{
pf_ratio(
  data,
  age,
  women,
  ceb,
  births,
  births_timing = c("end_of_period", "at_delivery"),
  k_ages = c(20, 25, 30),
  graph = TRUE
)
}
\arguments{
\item{data}{A data frame with the seven reproductive age groups (lower
bounds 15, 20, ..., 45).}

\item{age}{Column name for the (lower bound of the) age group.}

\item{women}{Column name for the number of women in each age group.}

\item{ceb}{Column name for children ever born in each age group.}

\item{births}{Column name for births in the year preceding the survey.}

\item{births_timing}{Either \code{"end_of_period"} (births in a 12-month period
classified by mother's age at the survey; the default) or \code{"at_delivery"}
(classified by age at the birth), selecting the Table 7 coefficient set.}

\item{k_ages}{Age-group lower bounds whose P/F ratios are averaged to form
the adjustment factor \code{K} (default \code{c(20, 25, 30)}).}

\item{graph}{Logical; if \code{TRUE} (default) a plot of the P/F ratios by age is
attached as \verb{$plot}; retrieve it with \code{plot()}.}
}
\value{
An object of class \code{dem_pf}: a list with the adjustment factor \code{K},
the unadjusted and adjusted total fertility rates \code{TFR} and \code{TFR_adjusted},
and a \code{table} of \code{P}, \code{f}, \code{phi}, \code{F}, the \code{PF} ratios, and the adjusted
rates \code{f_adjusted}.
}
\description{
Estimates the level of fertility by the Brass P/F ratio method (United
Nations, Manual X, Chapter II). The age pattern of current period fertility
is assumed reliable but its level suspect; the level is corrected using the
average parities of younger women, which are assumed accurate.
}
\details{
From the average parities \eqn{P(i) = CEB(i)/W(i)} and the period fertility
rates \eqn{f(i) = B(i)/W(i)}, the cumulated schedule
\eqn{\phi(i) = 5 \sum_{j \le i} f(j)} is formed and interpolated to the
parity-equivalent \eqn{F(i) = \phi(i-1) + a_i f(i) + b_i f(i+1) + c_i \phi(7)}
using the Coale-Trussell coefficients of Manual X Table 7. The adjustment
factor \eqn{K} is the average of the \eqn{P(i)/F(i)} ratios over the age
groups given by \code{k_ages} (20-34 by default, the most reliable), and the
adjusted total fertility rate is \eqn{K \times TFR}.
}
\examples{
# Manual X, Chapter II worked example: Bangladesh, 1974 (K = 1.500)
bd <- data.frame(
  age    = seq(15, 45, 5),
  women  = c(3014706, 2653155, 2607009, 2015663, 1771680, 1479505, 1135129),
  ceb    = c(1160919, 4901382, 9085852, 9910256, 10384001, 9164329, 6905673),
  births = c(320406, 609269, 561494, 367833, 237297, 95357, 38125)
)
pf_ratio(bd, age = "age", women = "women", ceb = "ceb", births = "births",
         graph = FALSE)

}
\references{
United Nations (1983). \emph{Manual X: Indirect Techniques for Demographic
Estimation}. New York: United Nations, Chapter II. Coale, A. J., & Trussell,
T. J. (1974). Model fertility schedules. \emph{Population Index}, 40(2),
185-258.
}
\seealso{
\code{\link[=chm_brass]{chm_brass()}}, \code{\link[=reproduction]{reproduction()}}
}
