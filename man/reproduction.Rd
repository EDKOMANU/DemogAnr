% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduction_stable.R
\name{reproduction}
\alias{reproduction}
\title{Reproduction Measures: GRR, NRR, and the Intrinsic Growth Rate}
\usage{
reproduction(
  data,
  age,
  asfr = NULL,
  births = NULL,
  women = NULL,
  nLx = NULL,
  radix = 1e+05,
  srb = 1.05,
  fraction_female = NULL,
  n = NULL,
  graph = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per reproductive age group.}

\item{age}{Column name for the (lower bound of the) age group.}

\item{asfr}{Column name for the age-specific fertility rate. Supply either
\code{asfr}, or both \code{births} and \code{women}.}

\item{births, women}{Column names for births and mid-year women, used to
form the rate when \code{asfr} is not given.}

\item{nLx}{Column name for the female life-table person-years \code{nLx}
(radix \code{radix}). Required for the NRR; if omitted only the GRR and TFR are
returned.}

\item{radix}{The life-table radix \eqn{\ell_0} of \code{nLx} (default 100000).}

\item{srb}{Sex ratio at birth (default 1.05), used to derive the default
\code{fraction_female}.}

\item{fraction_female}{Proportion of births that are female. Defaults to
\code{1/(1 + srb)}. Set to 1 if the supplied rates already refer to female
births only.}

\item{n}{Age-interval width(s); a scalar or per-row vector. Defaults to the
spacing of \code{age}.}

\item{graph}{Logical; if \code{TRUE} (default) a plot of the net maternity
function is attached and shown on printing.}
}
\value{
An object of class \code{dem_reproduction}: a list with \code{TFR}, \code{GRR},
\code{NRR}, \code{mean_age} (mean age of childbearing in the net schedule),
\code{r_approx} (\eqn{\ln(NRR)/A}), and a \code{table} of the per-age contributions.
}
\description{
Computes the gross and net reproduction rates from an age schedule of
fertility and a female life table, together with the total fertility rate,
the mean age of childbearing, and an approximate intrinsic growth rate.
}
\details{
The gross reproduction rate is \eqn{GRR = \sum_x n \cdot F^f_x}, the average
number of daughters a woman would bear if she survived through her
reproductive span; the net reproduction rate
\eqn{NRR = \sum_x F^f_x \cdot {}_nL_x / \ell_0} discounts that figure for
mortality. \eqn{F^f_x} is the female maternity rate, obtained from the
age-specific fertility rate by the fraction of births that are female.
}
\examples{
# Preston et al. (2001) Box 5.5: United States, 1991 (female births)
us1991 <- data.frame(
  age   = seq(10, 45, 5),
  fbir  = c(5816, 253979, 532712, 596823, 431694, 162005, 25531, 829),
  women = c(8620, 8371, 9419, 10325, 11125, 10344, 9496, 7188) * 1000,
  nLx   = c(494603, 493804, 492552, 491138, 489356, 486941, 483577, 478475)
)
reproduction(us1991, age = "age", births = "fbir", women = "women",
             nLx = "nLx", radix = 1e5, fraction_female = 1, graph = FALSE)

}
\references{
Preston, S. H., Heuveline, P., & Guillot, M. (2001). \emph{Demography:
Measuring and Modeling Population Processes}. Oxford: Blackwell Publishers.
Chapter 5.
}
\seealso{
\code{\link[=stable_population]{stable_population()}}
}
