% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marriage_health.R
\name{sullivan_hle}
\alias{sullivan_hle}
\title{Sullivan Health Expectancy}
\usage{
sullivan_hle(data, age, nLx, lx, prevalence, graph = TRUE)
}
\arguments{
\item{data}{A data frame with one row per age group, containing life-table
\code{nLx} and \code{lx} columns and an age-specific prevalence column.}

\item{age}{Column name for the age group.}

\item{nLx, lx}{Column names for the life-table person-years and survivors.}

\item{prevalence}{Column name for the prevalence of ill-health/disability
(a proportion between 0 and 1).}

\item{graph}{Logical; if \code{TRUE} (default) a plot of healthy vs. total life
expectancy by age is attached and shown on printing.}
}
\value{
An object of class \code{dem_sullivan}: a list with \code{HLE0} (health
expectancy at birth), \code{e0}, and a \code{table} of \code{ex}, \code{HLE}, \code{DLE}, and the
proportion of life spent healthy by age.
}
\description{
Partitions life expectancy into years spent in good and in poor health using
the Sullivan (1971) method: age-specific prevalence of ill-health or
disability is applied to the person-years of a period life table. The health
expectancy at age \eqn{x} is
\deqn{HLE(x) = \frac{1}{\ell_x} \sum_{a \ge x} (1 - \pi_a)\, {}_nL_a,}
where \eqn{\pi_a} is the prevalence of ill-health in age group \eqn{a}. By
construction \eqn{HLE(x) + DLE(x) = e_x}.
}
\examples{
lt <- lifetable(gphc2010, age = "Age", pop = "Pop", Dx = "Deaths",
                graph = FALSE)$lifetable
lt$disab <- pmin(0.6, 0.02 + 0.006 * (seq_len(nrow(lt)) - 1))
sullivan_hle(lt, age = "Age", nLx = "Lx", lx = "lx",
             prevalence = "disab", graph = FALSE)

}
\references{
Sullivan, D. F. (1971). A single index of mortality and morbidity.
\emph{HSMHA Health Reports}, 86(4), 347-354.
}
\seealso{
\code{\link[=lifetable]{lifetable()}}
}
