% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduction_stable.R
\name{stable_population}
\alias{stable_population}
\title{Stable Population Model}
\usage{
stable_population(
  data,
  age,
  asfr,
  nLx,
  radix = 1e+05,
  srb = 1.05,
  fraction_female = NULL,
  n = NULL,
  tol = 1e-09,
  max_iter = 100,
  graph = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per age group, spanning all ages.}

\item{age}{Column name for the (lower bound of the) age group.}

\item{asfr}{Column name for the age-specific fertility rate (0 outside the
reproductive ages).}

\item{nLx}{Column name for the female life-table person-years \code{nLx}.}

\item{radix}{The life-table radix of \code{nLx} (default 100000).}

\item{srb, fraction_female}{Sex ratio at birth and the derived (or supplied)
fraction of births that are female; see \code{\link[=reproduction]{reproduction()}}.}

\item{n}{Age-interval width(s); defaults to the spacing of \code{age}.}

\item{tol, max_iter}{Convergence tolerance and iteration cap for the Coale
procedure.}

\item{graph}{Logical; if \code{TRUE} (default) a plot of the stable age
distribution is attached and shown on printing.}
}
\value{
An object of class \code{dem_stable}: a list with the intrinsic rate \code{r},
birth rate \code{b}, death rate \code{d}, net reproduction rate \code{NRR}, \code{mean_age} of
the stable population, number of \code{iterations}, and a \code{table} of the stable
age distribution \code{cx}.
}
\description{
Solves for the intrinsic growth rate implied by a set of age-specific
fertility and mortality schedules, and derives the corresponding stable
population: the intrinsic birth and death rates, the stable age
distribution, and its mean age.
}
\details{
The intrinsic growth rate \eqn{r} is the root of Lotka's equation
\eqn{\sum_x e^{-r(x + n/2)}\, ({}_nL_x/\ell_0)\, F^f_x = 1}, found by Coale's
iterative procedure. The stable birth rate is then
\eqn{b = 1 / \sum_x e^{-r(x + n/2)}\, {}_nL_x/\ell_0}, the death rate is
\eqn{d = b - r}, and the stable age distribution is
\eqn{{}_nc_x = b\, e^{-r(x + n/2)}\, {}_nL_x/\ell_0}.
}
\examples{
# Preston et al. (2001) Box 7.1: Egypt, 1997 (intrinsic r = 0.01424)
egypt <- data.frame(
  age  = seq(15, 45, 5),
  Lx   = c(4.66740, 4.63097, 4.58518, 4.53206, 4.46912, 4.39135, 4.28969),
  ma   = c(0.00567, 0.06627, 0.11204, 0.07889, 0.05075, 0.01590, 0.00610)
)
stable_population(egypt, age = "age", asfr = "ma", nLx = "Lx",
                  radix = 1, fraction_female = 1, graph = FALSE)$r

}
\references{
Preston, S. H., Heuveline, P., & Guillot, M. (2001). \emph{Demography:
Measuring and Modeling Population Processes}. Oxford: Blackwell Publishers.
Chapter 7. Coale, A. J. (1957). A new method for calculating Lotka's r.
\emph{Population Studies}, 11(1), 92-94.
}
\seealso{
\code{\link[=reproduction]{reproduction()}}, \code{\link[=lifetable]{lifetable()}}
}
