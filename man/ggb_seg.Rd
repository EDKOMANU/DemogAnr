% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/death_distribution.R
\name{ggb_seg}
\alias{ggb_seg}
\title{Combined Growth Balance and Extinct Generations Estimate}
\usage{
ggb_seg(
  data,
  age,
  pop1,
  pop2,
  deaths,
  t1,
  t2,
  deaths_annual = FALSE,
  age_range = c(5, 65),
  open_mean_age = NULL,
  graph = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per age group.}

\item{age}{Column name for the lower bound of each age group.}

\item{pop1, pop2}{Column names for the population enumerated at the first and
second census.}

\item{deaths}{Column name for deaths by age. Taken as total deaths over the
intercensal period unless \code{deaths_annual = TRUE}.}

\item{t1, t2}{Decimal years of the two censuses (for example 2000.3 and
2010.4).}

\item{deaths_annual}{Logical; \code{TRUE} if \code{deaths} are already an annual
average rather than the intercensal total (default \code{FALSE}).}

\item{age_range}{Two ages for both stages (default \code{c(5, 65)}).}

\item{open_mean_age}{Mean age at death in the open-ended interval. When
\code{NULL} (default) it is taken as \eqn{x + 1/M}, assuming deaths decline
exponentially above the open age. Using the interval's nominal midpoint
instead biases the completeness downwards by several per cent, so this is
worth setting where the open age is low or mortality there is known.}

\item{graph}{Logical. If \code{TRUE} (default), the diagnostic plot of the fitted
line is attached as \verb{$plot}; retrieve it with \code{plot()}.}
}
\value{
An object of class \code{dem_ggb_seg}: a list with the final
\code{completeness}, the \code{coverage_ratio} from the growth balance stage, and
the two component fits, \code{ggb} and \code{seg}, the latter run on the adjusted
censuses.
}
\description{
Runs the two death distribution methods in the sequence recommended by Hill,
You and Choi (2009): the generalized growth balance first, to estimate how
the coverage of the two censuses differs, and then synthetic extinct
generations on censuses adjusted for that difference. \code{\link[=seg]{seg()}} on its own
assumes the two censuses enumerate equally well, which is what this
correction removes.
}
\examples{
# A population whose deaths are registered at 70 per cent completeness.
# The censuses are ten years apart and enumerate equally well, so all three
# methods should return a completeness near 0.70.
d <- data.frame(
  age = c(0, seq(5, 85, 5)),
  n1  = c(41255, 36289, 31948, 28121, 24743, 21758, 19116, 16770, 14677,
          12798, 11094, 9527, 8060, 6659, 5298, 3970, 2706, 2614),
  n2  = c(52973, 46596, 41022, 36108, 31770, 27938, 24545, 21533, 18846,
          16433, 14245, 12233, 10349, 8550, 6802, 5098, 3475, 3356),
  dth = c(516, 134, 126, 123, 126, 136, 154, 184, 229, 294, 385, 508,
          668, 865, 1084, 1282, 1380, 2833)
)
ggb_seg(d, age = "age", pop1 = "n1", pop2 = "n2", deaths = "dth",
           t1 = 2000, t2 = 2010, graph = FALSE)

}
\references{
Hill, K., You, D., & Choi, Y. (2009). Death distribution methods for estimating adult mortality. \emph{Demographic Research}, 21, 235-254. \doi{10.4054/DemRes.2009.21.9}

Moultrie, T., Dorrington, R., Hill, A., Hill, K., Timaeus, I., & Zaba, B. (Eds.). (2013). \emph{Tools for Demographic Estimation}. Paris: IUSSP. \url{https://demographicestimation.iussp.org/}
}
\seealso{
\code{\link[=ggb]{ggb()}}, \code{\link[=seg]{seg()}}
}
